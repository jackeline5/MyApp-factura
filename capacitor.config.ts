import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Sistema_Factura',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
